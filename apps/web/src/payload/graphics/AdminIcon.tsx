"use client";

import React from "react";

/** Sidebar / shell mark — same asset family as the marketing site favicon mark. */
export default function AdminIcon() {
  return (
    <img
      src="/brand/dch-mark.svg"
      width={28}
      height={28}
      alt=""
      style={{ display: "block", borderRadius: "6px" }}
    />
  );
}
